#include "main.h"
/**
 * _strcat - concatenates strings
 * @dest: destination
 * @src: input
 * Return: concated strin
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
